namespace ARSlotcar
{
    /// <summary>
    /// コネクタの種類。現状は全パーツ共通の1種類のみで運用する想定だが、
    /// 将来「特殊な接続口だけ持つパーツ」を追加する場合に備えて種類を分けられるようにしておく。
    /// </summary>
    public enum ConnectorType
    {
        Standard
    }
}
